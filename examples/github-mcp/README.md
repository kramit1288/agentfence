# GitHub MCP Demo Assets

Use these files with the walkthrough in `docs/demos/github-mcp.md`.

- `config.json` configures the local AgentFence HTTP listener.
- `policy.yaml` defines the allow, require-approval, and deny rules.
- `requests/` contains sample MCP JSON-RPC request bodies for the demo.